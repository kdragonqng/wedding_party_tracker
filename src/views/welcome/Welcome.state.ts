import 'reflect-metadata';
// import { container } from 'tsyringe';
import { ComponentState } from '../../common/types/componentState';

export class WelcomeState extends ComponentState {

    public async init(): Promise<void> {
        // const me = this;
    }
}